server:
  port: 8080
#nats.uri: ${NATS_URI}

nats:
  spring:
    io:
      nats:
        client:
          servers: nats://localhost:4222

grpc:
  server:
    port: 9091
    security:
      enabled: false
