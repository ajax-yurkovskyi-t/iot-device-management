logging:
  level:
    org.springframework.test.context.cache: DEBUG

mongock:
  enabled: false

nats.uri: nats://localhost:4222

spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      properties:
        session.timeout.ms: 10000
        max.poll.interval.ms: 10000
    bootstrap-servers: http://localhost:9092
